// Only `svelte-check` and the Svelte VS Code extension read this — Mochi applies
// these options itself. See `mochi-framework/svelte.config.js`.
// Kept in sync with `packages/minimal/svelte.config.js`.
export { default } from 'mochi-framework/svelte.config.js';
