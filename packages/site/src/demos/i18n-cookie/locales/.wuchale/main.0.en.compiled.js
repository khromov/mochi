/** @type import('wuchale').CompiledElement[] */
export let c = ["English","Svenska","Українська","Good day, and welcome to Mochi!","This panel re-translates itself instantly when you switch languages — no page reload.","Your choice is stored in a cookie, so the server renders this page in your language next time you visit.","Choose your language","Language",["Active language code: ",[0]],"Internationalization (cookies)","A cookie holds the visitor's language. Mochi reads it during SSR so the page renders translated, and the hydrated island switches language in place — then writes the cookie so the choice sticks on the next request."]