<p class="ff-note">Heads up: Firefox doesn't currently support the cross-document View Transitions API, so navigations there swap instantly without animating.</p>

<style>
  .ff-note {
    margin: 1.2rem 0 0;
    padding: 0.6rem 0.8rem;
    border-radius: 8px;
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent);
    background: var(--surface-subtle, rgba(0, 0, 0, 0.03));
    font-size: 0.85rem;
    color: var(--text-muted);
  }
</style>
